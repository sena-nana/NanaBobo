# NanaBobo 业务边界

## 当前纵切

首页只连接两项真实能力：

1. B站二维码登录、账号状态和退出登录。
2. 按直播间号查询公开房间信息。

Rust 侧持有 B站请求和登录 Cookie，Vue 侧只消费 `src/contracts/bilibili.ts` 中的脱敏契约。普通设置可以使用 `tauri-plugin-store`，但登录态只能进入 OS Keyring。

## 后续模块

弹幕显示、长连接重连、直播录制、事件整理和本地回放需要先定义事件顺序、背压、持久化和恢复契约。契约与真实数据链路完成前，不添加对应的路由、侧边栏入口或禁用按钮。
